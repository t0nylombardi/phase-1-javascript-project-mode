
export const Card = function(person) {
  return (
    <div className="card">
      <img src={this.image[0].large} alt={this.title} />
      <h2>{wantedCard.name}</h2>
      <p>{wantedCard.description}</p>
    </div>
  );
}